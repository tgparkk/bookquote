# 2026-05-12~13 — 화면 설계 마무리 + Stage 2 구현 착수 (PR1~4 + 책 별점)

이 세션: 화면 설계 Phase B 마무리(그룹 1·2·3 문서 + `screens.html` + `flows.md`/`client-architecture.md` V1.5 정정) → V1 사용 시나리오 walkthrough → 매니저 모드로 미해결 결정 일괄 → **Stage 2 구현 시작: PR1·2·3·4 + 책 별점** → 릴리스 APK 빌드·실기기 설치.

## 결정 (DECISIONS.md)
- 2026-05-11: 내장 OCR 안 함(폰 기능 + 클립보드 붙여넣기) / 오프라인 = 경량 로컬 아웃박스(완전 동기화 엔진 V1.5)
- 2026-05-12: 카드 텍스트 앵커 V1.5(V1은 폰트±·정렬만) / 표지 없는 책 T4 비활성화 / 인용구 목록 = 서재 탭 세그먼트(별도 탭 X) / 인용 AI는 "AI" 단어·"곧 출시" 약속 안 함 / 홈 "받은 카드 함" V1.5(V1 홈 = 내 인용 피드) / `listMyQuotes` cursor 시그니처 확정
- 2026-05-13: 책 별점 = `user_books.rating` 정수 1~5(nullable) / 반쪽 별 V1.5

## 산출물

### 화면 설계 (Stage 0b 연장 — 2026-05-12 완료)
- `docs/design/screens/*.md` 13개 (7섹션 구조) + `README.md`(인벤토리+IA) + `docs/design/mockups/screens.html`(전 13화면 와이어프레임) + `walkthrough.html`(V1 사용 시나리오 8단계)
- `docs/discovery/competitor-screen-analysis-2026-05-11.md` + `competitor-references.html`
- `flows.md`·`client-architecture.md` 상단에 V1.5 범위 정정 배너

### Stage 2 구현 (커밋 순서)
| 커밋 | 내용 |
|---|---|
| `a0bcebf` | **PR1** 인용구 데이터 레이어 — `quotes` 테이블 마이그레이션(remote 적용), `quote.dart`/`quote_repository`/`quote_outbox`/`quote_providers`, pubspec(`shared_preferences`·`connectivity_plus`), `build_runner`로 freezed/json 생성. 테스트 7개 |
| `27690a3` | **PR2** 인용구 입력 화면 — 본문+카운터+클립보드 붙여넣기 배너+책 연결+페이지·무드 칩+draft 자동저장/복원+PopScope 폐기 확인+"카드 만들기→"/"저장만"+오프라인 큐잉. `mood_chips.dart`(moodColors 단일 정의처)·`quote_draft.dart`. `book_search_sheet`의 잘못된 "서재 추가" 토스트 제거. 테스트 3개 |
| `ab240b5` | **책 별점** — `user_books.rating` 마이그레이션(remote 적용), `setMyRating`/`getMyRating`, `myRatingProvider`, `StarRating` 위젯, 책 상세 헤더 별점 행(로그인 시만) + raw `$e` 제거. 테스트 4개 |
| `184c23f` | walkthrough.html |
| `e243398` | **PR3** 홈 화면 재작성 — "내 인용 피드": `quote_feed_provider`(cursor-after 무한스크롤 누적 Notifier + 낙관적 삭제), `listMyQuotesWithBook`(책 임베드, N+1 회피), `quote_list_card`(홈·인용목록 공유), `home_screen` 재작성(RefreshIndicator + 무한스크롤 + 빈상태 CTA + 삭제 확인 + 포그라운드 복귀 시 아웃박스 flush). `quote_input` 저장 시 피드 invalidate. 테스트 3개 |
| `9e203ee` | **PR4** 서재 "책↔인용구" 세그먼트 — `library_screen`(stub→ConsumerStatefulWidget, SegmentedButton, `?tab=quotes&mood=` 쿼리, `_ErrorView` raw `$error` 제거+[다시 시도]), `quote_list_view.dart`(무드 필터 칩+개수, cursor 무한스크롤, 빈상태 분기, 삭제), `my_quote_mood_counts()` RPC(remote 적용) + `getMoodCounts/parseMoodCounts`. 테스트 2개 |
| `27437b8`·`20e1fda` | (앞선) 화면 설계 문서 묶음 + screens.html 그룹2·3 + flows/client-arch V1.5 정정 |

**현황**: `flutter analyze` clean · `flutter test` 25개 통과 · 마이그레이션 4개(`quotes`·`user_books.rating`·`my_quote_mood_counts` + Stage1 4개) 원격 적용 확인 · main에 push.

## 발견·수정한 코드 버그 (구현하며 함께 처리)
- `book_search_sheet._onPick`의 "서재에 추가했어요" 토스트 제거(실제론 books 카탈로그 upsert일 뿐) ✅
- `book_detail_screen` raw `$e` 노출 제거 ✅
- `library_screen._ErrorView` raw `$error` 제거 + [다시 시도] 버튼 ✅, 추가 실패 SnackBar userMessage화 ✅
- `book_detail_screen` null-guard 정리 ✅
- 남은 것(백로그): 로그인 매직링크 재전송 출구·`?from=` 보존, 콜백 타임아웃 사유 안내, 책 검색 시트 검색-전 빈결과·ISBN 직접 등록·오프라인 캐시-우선, `deep_link_handler` 일반화, `me_screen` 긴 이메일 오버플로·빈 onTap, 스플래시 워드마크

## 다음 세션
**PR5 (Me 화면 보강)** — 출시 블로커 포함(in-app 계정 삭제 Edge Function, 이용약관·개인정보처리방침 호스팅 URL). 상세는 `STAGES.md`의 "▶ 다음 세션 시작점" 참조. 그다음 PR6(책 상세 보강 + deep_link_handler), 그다음 Stage 3(카드 에디터·공유).

---
**산출 파일**: `docs/design/screens/*.md`(13)·`README.md` / `docs/design/mockups/{screens,walkthrough}.html` / `docs/discovery/competitor-screen-analysis-2026-05-11.md`·`mockups/competitor-references.html` / `lib/features/quote/**`·`lib/features/home/home_screen.dart`·`lib/features/library/library_screen.dart`·`lib/features/book/{book_detail_screen.dart,data/book_repository.dart,state/book_providers.dart,presentation/widgets/star_rating.dart}` / `supabase/migrations/2026051212~14*.sql` / `test/features/{quote,home,book}/*` / `docs/{DECISIONS,STAGES}.md` 갱신 / `docs/discovery/{flows,client-architecture}.md` V1.5 배너 / 세션 로그 2개
