<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Supabase 호출, 알라딘 책 검색, 표지 이미지 fetch 등 모든 네트워크에 필요.
         debug/profile manifest에는 Flutter 툴이 자동으로 넣지만 release는 명시해야 한다. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- PR-PB: FCM 푸시 알림 표시(Android 13+ 런타임 권한). 코드에서 맥락에 맞춰
         requestPermission()로 요청한다. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- PR16-A: allowBackup="false" — flutter_secure_storage에 든 E2EE 마스터키가
         Google Drive autoBackup으로 새는 걸 막는다. 안 막으면 "키를 서버가 모름"
         보장이 무력화. user data는 Supabase가 source of truth이므로 백업 차단해도
         새 폰 = 다시 로그인이면 복구됨. DECISIONS 2026-05-17 E2EE 항목. -->
    <application
        android:label="책글귀"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- 인앱 deep link: io.github.tgparkk.bookquote://book/:id?from=share
                 (공유 카드 → 책 상세, K-factor). 매직링크/OAuth callback은 V1에서 제거됨
                 — OAuth는 각 SDK가 자체 채널로 처리. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="io.github.tgparkk.bookquote" />
            </intent-filter>
            <!-- HW-A: home_widget 탭 → 앱 실행(HomeWidgetLaunchIntent). 위젯 클릭 데이터를
                 Dart에서 받아 딥링크로 라우팅하는 것은 HW-B. -->
            <intent-filter>
                <action android:name="es.antonborri.home_widget.action.LAUNCH" />
            </intent-filter>
        </activity>

        <!-- HW-A: 홈 화면 위젯 '이 책의 한 줄' provider. 데이터는 home_widget이
             SharedPreferences로 전달하며, 미설정 시 Kotlin 기본값이 렌더된다. -->
        <receiver
            android:name=".BookQuoteWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/bookquote_widget_info" />
        </receiver>

        <!-- HW-D: 홈 화면 위젯 '읽고 있는 책' provider (표지 + N일째). -->
        <receiver
            android:name=".ReadingBookWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/reading_book_widget_info" />
        </receiver>
        <!-- PR21: 카카오 OAuth Custom Tabs 콜백 액티비티.
             scheme `kakao{NATIVE_APP_KEY}`는 빌드 시 strings.xml의
             `kakao_native_app_key`를 참조하도록 manifestPlaceholder로 주입한다
             (app/build.gradle.kts의 manifestPlaceholders 참고). -->
        <activity
            android:name="com.kakao.sdk.flutter.AuthCodeCustomTabsActivity"
            android:exported="true">
            <intent-filter android:label="flutter_web_auth">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="kakao${KAKAO_NATIVE_APP_KEY}" android:host="oauth"/>
            </intent-filter>
        </activity>
        <!-- PR-PB: FCM 백그라운드/종료 알림이 사용할 기본 채널 id. PushService가 앱
             기동 시 동일 id('bookquote_high')로 고중요도 채널을 생성한다 — 삼성 One UI
             등에서 채널 명시가 없으면 알림이 표시 누락되는 문제 방지. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="bookquote_high" />
        <!-- AdMob 앱 ID (홈 하단 배너, 2026-07-03 수익모델 확정). 이 meta-data가 없으면
             SDK 초기화 시점에 크래시한다. 광고 단위 ID는 lib/features/ads/ad_ids.dart. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-7230084799824817~1381070822" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher: 외부 브라우저(이용약관/개인정보처리방침) + 메일 앱(문의하기) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="mailto" />
        </intent>
    </queries>
</manifest>
